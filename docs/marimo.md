# Marimo Suraya

## Usage

Build the OCI image.
```shell
uv run -m suraya.marimo.mk build
```

Invoke the web server (standalone).
```shell
uv run -m suraya.marimo.mk run
```

Invoke the web server (embedded).
```shell
uv run -m suraya.marimo.serve
```
